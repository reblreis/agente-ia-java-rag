spring:

  application:
    name: api-rag

  datasource:
    url: jdbc:postgresql://localhost:5435/bd_rag
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  mongodb:
    host: localhost
    port: 27018
    database: historico_ia
    username: ${MONGO_USER}
    password: ${MONGO_PASSWORD}
    authentication-database: admin

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-5-mini
      embedding:
        options:
          model: text-embedding-3-small

    vectorstore:
      pgvector:
        initialize-schema: true
        index-type: HNSW
        distance-type: COSINE_DISTANCE

server:
  port: 8081

tokens:
  base: 1000000.0
  preco:
    entrada: 0.25
    saida: 2.00